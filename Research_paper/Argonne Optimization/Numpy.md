
# Numpy

```python
"""
NumPy 文件内方法总结（仅包含PDF中出现的方法）
==================
"""

import numpy as np

# ==================== 1. 数组创建 ====================

np.array([1, 2, 3]：list) -> np.ndarray                    # 从列表创建数组
np.arange(10)                          # 创建序列 [0,1,...,9]
np.arange(1, 3, 0.4)                   # 指定起点、终点、步长（支持小数）
np.linspace(0, 100, 7)                 # 等分指定数量个点
.reshape(3, 4)                         # 改变数组形状


# ==================== 2. 数组属性 ====================

arr.dtype          # 元素数据类型
arr.shape          # 数组形状 (行,列)


# ==================== 3. 索引与切片 ====================

arr[2]             # 单元素索引
arr[1][2]          # 多维索引（列表方式）
arr[1, 2]          # 多维索引（元组方式，推荐）
arr[1:4]           # 切片 [start:stop]
arr[3:10:2]        # 切片带步长 [start:stop:step]
arr[:, 1]          # 取整列
arr[1:4, 0:2]      # 多维度切片


# ==================== 4. 花式索引 (Fancy Indexing) ====================

arr[[1, 3, 6]]                      # 用列表索引多个元素
arr[mask]                           # 用布尔数组（掩码）索引
arr[arr > 100]                      # 条件筛选（结合比较运算符）
arr[arr % 2 == 1] = -1              # 条件赋值


# ==================== 5. 数组运算 ====================

# 算术运算（同时对每个元素元素）
arr + 2            # 加
arr - 1            # 减
arr * 2            # 乘
arr / 2            # 除
arr ** 2           # 幂运算

# 比较运算（返回布尔数组）
arr > 100
arr == 0

# NumPy数学函数（元素级）
np.sin(arr)
np.cos(arr)


# ==================== 6. 矩阵运算 ====================

np.dot(A, B)       # 矩阵乘法
la.det(A)          # 行列式（numpy.linalg.det）
la.inv(A)          # 逆矩阵（numpy.linalg.inv）
la.solve(A, B)     # 解线性方程组（numpy.linalg.solve）


# ==================== 7. 聚合/归约方法 (Reductions) ====================

np.sum(arr)                    # 全局求和
np.sum(arr, axis=0)            # 按轴求和（axis=0:沿列，axis=1:沿行）
arr.mean()                     # 平均值
arr.mean(axis=0)               # 按轴平均
arr.std()                      # 标准差


# ==================== 8. 广播 (Broadcasting) ====================

# 自动扩展不同形状的数组进行运算
# 标量与数组：arr * 4
# 不同维度：(4,3) + (3,) → (4,3)


# ==================== 9. 文件中出现但未详细展开的方法 ====================

np.matrix()        # 创建矩阵（文件提到但不推荐）
np.append()        # 追加元素（注意：创建新数组，非原地修改）
np.zeros()         # 创建全0数组（仅示例中出现）
np.ones()          # 创建全1数组（仅示例中出现）
```